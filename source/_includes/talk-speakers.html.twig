{% if page.speakers %}
    <h2>Speakers</h2>

    <ul>
        {% for speaker in data.speakers if speaker.shortname in page.speakers %}
            <li>
                <a href="{{ speaker.url }}">{{ speaker.title }}</a>
            </li>
        {% endfor %}
    </ul>
{% endif %}
